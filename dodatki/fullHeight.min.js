jQuery(window).on("load resize",function(){var windowHeight = jQuery(window).height(), windowWidth = jQuery(window).width(), offsetHeight = 0, maxHeight = 730, minHeight = 300; borderHeight = 650; if (Modernizr.touch) {offsetHeight = 70; } jQuery('#bgVideo, .fullHeight').css('height', function() {if (windowHeight < maxHeight && windowHeight > minHeight) {return windowHeight - offsetHeight; } else if (windowHeight < minHeight) {return minHeight - offsetHeight; } else {return maxHeight - offsetHeight; } }); });